
{% extends 'layout.twig' %}

{% block content %}
    <h1>{{ title }}</h1>
    <div class="row">

        {% for row in data %}
            <div class="col-md-15">
                {{ row }}
            </div>
        {% endfor %}


    </div>
{% endblock %}