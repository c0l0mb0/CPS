{% extends 'layout.twig' %}

{% block content %}
    <h1>{{ title }}</h1>
    <div class="row">

        {% for row in data %}
            <div class="col-md-4">
                <br>{{ row }}</br>
            </div>
        {% endfor %}


    </div>
{% endblock %}